const Homepage = () => {
    return (
        <div className="homepage-wrapper">
            <h2>Movie/Series search engine based on the OMDb API</h2>
            An API key is needed in components App, MovieDetails for the requests to work
            <p />Minimum search string length: 3 characters
        </div>

    )
}

export default Homepage;